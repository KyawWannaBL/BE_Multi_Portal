Implemented:
- live-loaded filter dropdown options in AdminShellPage
- backend-driven select options for branch, merchant, deliveryman
- shared filter option endpoints in adminShellScreens.ts
- fallback still works if endpoint returns no options
