Implemented:
- safe JSON fallback for remaining admin sidebar endpoints
- prevents HTML SPA fallback from crashing list/report pages
- covers deliverymen, accounting, reporting, broadcast message, teams, contacts, settings
- keeps UI stable until live backend endpoints are wired
