Implemented:
- safe JSON fallback for all way-management admin endpoints
- prevents HTML index page responses from crashing report pages
- improved native date picker behavior with showPicker support
- date preset now auto-populates dateFrom and dateTo in the UI
- fallback filter-option endpoints added for branches, merchants, deliverymen
