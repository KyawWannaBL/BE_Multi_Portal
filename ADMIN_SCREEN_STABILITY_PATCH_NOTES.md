Implemented:
- formShell pages now render form components without GET fetch
- safe JSON fallback for missing admin endpoints
- create screens marked as real form shells where possible
- fixes 404/HTML JSON crashes across merchants, create delivery, accounting, reporting, broadcast, teams
