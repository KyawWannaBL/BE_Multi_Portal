Implemented:
- Create Delivery now renders as a true form shell
- added full formFields for delivery creation
- added submitEndpoint for create-delivery
- added live merchant filter option endpoint binding for merchant_id
- prevents AdminShellPage from falling into report JSON fetch mode for form shells
